#!/usr/bin/env bash
set -euo pipefail

if [[ "${CI:-}" == "true" ]]; then
  npm ci
elif command -v sfw >/dev/null 2>&1; then
  sfw npm install
else
  echo "Socket Firewall is required for local dependency installation." >&2
  exit 1
fi
